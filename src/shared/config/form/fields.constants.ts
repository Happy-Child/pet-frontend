export enum INPUT_TYPES {
  TEXT = 'text',
  NUMBER = 'number',
}

export enum FORM_FIELDS {
  EMAIL = 'email',
  PASSWORD = 'password',
}

export enum FORM_LABELS {
  EMAIL = 'Email',
  PASSWORD = 'Password',
}
